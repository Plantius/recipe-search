[project]
name = "recipe-search"
version = "0.1.0"
description = "A self-hosted web-app to search for recipes locally."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.136.3",
    "pre-commit>=4.6.0",
]
